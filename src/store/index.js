import { createStore } from "vuex";
import * as crates from "./modules/crates.js";

export default createStore({
  state: {},
  getters: {},
  mutations: {},
  actions: {},
  modules: {
    crates,
  },
});
